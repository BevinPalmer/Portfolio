// Optional manual ordering for retouching pairs.
// Use the `id` from getImagePairs() (kebab-case slug before -before/-after), e.g. "nakedwolf-boots".
export const retouchingOrder = ["dovehoodie"];

// Optional: show only these ids. If empty, all pairs render.
export const retouchingFeatured = [];
